<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.5//EN"
        "http://struts.apache.org/dtds/struts-2.5.dtd">

<struts>
    <constant name="struts.enable.DynamicMethodInvocation" value="false" />
    <constant name="struts.devMode" value="true" />
    <!-- 只想用REST 就配置这个-->
<!--    <constant name="struts.mapper.class" value="rest" />-->

    <!-- REST和非REST风格一起用， 就配置这个-->
    <constant name="struts.action.extension" value="xhtml,,xml,json,action"/>

    <!-- 以上可以不设，不论哪种，都要设定以下项目 -->
    <!-- XXXAction类后缀，可以是Controller，那么所有控制器后缀都设为Controller-->
    <constant name="struts.convention.action.suffix" value="Controller" />
    <constant name="struts.convention.action.mapAllMatches" value="true" />
    <constant name="struts.convention.default.parent.package" value="monkey-rest" />
    <!-- Action类所在包名 -->
    <constant name="struts.convention.package.locators" value="action"/>
    <package name="monkey-rest" extends="rest-default" namespace="/rest"/>
</struts>